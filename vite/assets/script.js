// --- Scripting.
// page bw
// const thiswebsite = document.querySelector('img');
// thiswebsite.style.filter = localStorage.getItem('style');

// Fonction qui applique les couleurs au site.
// function ft_bw (style) {
//     switch (style.id) {
//         case 1:
//             thiswebsite.style.filter = `grayscale(0)`;
//             break;
//         case 0:
//             thiswebsite.style.filter = `grayscale(1)`;
//             break;
//         default:
//             thiswebsite.style.filter = `grayscale(1)`;
//     }
//     // Met a jour le Cookie d'accessiblité
//     localStorage.setItem('style', thiswebsite.style.filter);
// }